/**
 * @typedef {object} Theme
 * @property {string} title - Page title to show in app bar
 * @property {string} logo - URL to logo shown in app bar
 */

export { default as defaultTheme } from './defaultTheme';